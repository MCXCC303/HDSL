.gradle/
build/
*.class
.idea/
*.iml
out/
dist/

# Working notes, verification captures and references. Not part of the product: the plan is a
# scratch pad, the screenshots are evidence for one review, the reference images belong to HMCL.
.dev/
.verify/
# Everything under docs/ stays out of the product except the plugin guide, which is written
# for other people rather than for this desk.
docs/*
!docs/plugin-guide/

# build outputs, but keep the vendored JFoenix patch base
*.jar
!lib/JFoenix.jar
!gradle/wrapper/*.jar
